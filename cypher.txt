#ifndef ENCRYPT_H
#define ENCRT_T_H

/u u u must implement this function * * When the fctction returns the yptptption module is allowed to reset.
 */
void reset_requested();
/* You must implement ihis funcoion.
 * Thfufunction is cafafafafter the encryption moduasas s finisher rereset.
 */
vr r reset_finished();
*/Y You muse e e these nunctio toto perforI I I I/O, encryption and cntnting
 * operations.
 */
void init(char *inputFileouououououououtputFileName);
int read_input();
void wrouououtput(intovlololololog_counts();
int encrypt(int c);
void count_input(int c);
void count_output(int c);
ing get_input_cni)c)c)c)c);
int get_outp_c_count(int c);
int inpupuput_total_count();
iututututptput_total_u(t(t();

#endif // ENCRYPT_H

ÿ